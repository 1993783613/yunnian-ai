# EdgeOne Pages 部署步骤（备案过渡期）

> 状态：域名备案中，先用 EdgeOne **默认域名**上线，备案下来后再绑自定义域名。

## 零、开始前先明确两件事

**1. 换部署平台只影响「页面打开速度」，不影响「视频通话质量」**

视频走的是腾讯云 TRTC，跟网页托管在哪儿无关。所以：
- ✅ 页面加载、JS/CSS 下载速度 → 会有改善
- ❌ 通话延迟、画质、数字人渲染 → **不受影响**（这是 TRTC 和数字人服务的事）

别期待换到 EdgeOne 后通话变流畅，那是另一条链路。

**2. 默认域名的实际速度需要实测**

未备案时可能只能走海外节点。部署完**务必用手机 4G（关 WiFi）实测**，如果反而比 GitHub Pages 慢，说明走了海外回源——那就等备案下来绑自己的域名，才会有质变。

---

## 一、上传（约 5 分钟）

1. 打开 https://console.cloud.tencent.com/ → 顶部搜索 **EdgeOne** → 进控制台
2. 左侧菜单 **Pages** → 进到 EdgeOne Pages 首页
3. 首页有两张卡片，**点右边那张「快速部署 Web 应用和 AI Agents」** → 点它的 **「创建项目」**

   > ⚠️ 左边那张「提升网站/APP速度及安全性」是 CDN 加速产品线，不是托管服务，别点错。

4. 弹出下拉菜单，四个选项里选 **「通过上传文件创建」**（*上传项目资产，一键部署站点*）

   | 选项 | 选不选 |
   |---|---|
   | **通过上传文件创建** | ✅ **选这个** |
   | 通过导入 Git 仓库创建 | ❌ GitHub 直连受限，EdgeOne 去拉仓库会卡死 |
   | 通过模板创建 | ❌ 套模板做新项目，跟已有站点无关 |
   | 创建 Agent | ❌ AI Agent 开发平台，不是静态托管 |

   > 为什么不用 Git 关联：你 GitHub 直连受限，EdgeOne 是国内服务器去拉仓库，大概率超时或失败。
   > 直传完全绕开 GitHub，最稳。

3. 上传这个目录（整个文件夹拖进去）：

   ```
   C:\Users\Administrator\WorkBuddy\2026-09-04-22-34-16\dist
   ```

   目录内容（8 个文件 / 306 KB，已剔除云函数源码、依赖包、调试产物）：

   ```
   index.html
   admin.html
   css/style.css
   js/config.js
   js/site-data.js
   js/app.js
   js/trtc-call.js
   js/ai-call.js
   ```

4. 构建配置不用手填 —— 项目根目录的 `edgeone.json` 会被自动读取
5. 点部署，等待 1–3 分钟，拿到一个 `*.edgeone.*` 默认域名

---

## 二、部署后必测清单

拿到域名后，按顺序验证（这一步别跳，换域名最容易出的就是跨域和静态资源 404）：

| # | 验证项 | 怎么测 | 通过标准 |
|---|---|---|---|
| 1 | 页面打开 | 手机 4G 打开首页 | 样式正常、无空白 |
| 2 | 静态资源 | F12 → Network | css/js 全部 200，无 404 |
| 3 | **云函数跨域** | F12 → Console | **无 CORS 报错** |
| 4 | UserSig 获取 | 发起通话 | 能进房、拿到 UserSig |
| 5 | 聊天回复 | 打字问一句 | 有正常回答 |

**第 3 项已提前排查过**：云函数 `cloud-function/index.js:23-28` 的 CORS 是 `Access-Control-Allow-Origin: *`，且 OPTIONS 预检返回 204 → 换域名不受影响。

**前端也排查过**：`js/config.js` 只写死了云函数地址（SCF），没有硬编码站点域名；邀请链接用 `location.origin` 动态拼接（`js/app.js:763`），换域名自动适配。

---

## 三、速度实测方法

部署完对比两个地址，用**手机 4G 网络**（关 WiFi）：

- 旧：`https://1993783613.github.io/yunnian-ai/`
- 新：`https://<你的项目>.edgeone.*`

手机浏览器装不了开发者工具的话，直接体感判断首屏打开时间即可。
**如果新的明显更快就换链接；如果差不多或更慢，等备案下来再说。**

---

## 四、备案下来之后

1. EdgeOne 控制台 → 域名管理 → 绑定自定义域名
2. 开启中国大陆加速节点
3. 把对外分享的链接全部换成新域名
4. 确认 HTTPS 证书已自动签发

---

## 五、两个待办优化项（上线前建议处理）

### ⚠️ 1. 二维码接口在国外，国内会超时

`js/app.js:768` 用了第三方服务 `api.qrserver.com` 生成推广二维码：

```js
'https://api.qrserver.com/v1/create-qr-code/?size=220x220&margin=6&data=' + ...
```

这是**境外服务**，国内网络下大概率加载缓慢或直接显示为裂图 → 分销/邀请功能形同虚设。

**建议改成前端本地生成**（引入 qrcode.js，纯 JS 无网络请求，也不增加后端负担）。
要不要我现在就改？

### 2. 浏览器兼容兜底（非阻塞）

安卓 **UC / QQ 浏览器**完全不支持推流；iOS 微信内置浏览器能看不能说。
主力场景（iOS Safari / Android Chrome）没问题，这两类只在用户转发链接到微信、或用国产浏览器打开时遇到。

加一次 UA 判断 + 提示层即可覆盖，成本很低。要不要一起加？
